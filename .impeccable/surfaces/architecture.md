# Architecture product showcase

Mode: Experience, with a readable scientific explanation. Existing approved OceanEmbed visual world; code-led implementation. User explicitly chose a labeled 3D/exploded product presentation inspired by the landing architecture CTA and authorized execution.

First viewport: pure black, oversized Manrope title on the left, a large monochrome 3D exploded learning assembly on the right. Fine leader lines connect actual layers to readable selectable labels. The assembly separates temporal input, fusion, encoder, embedding, decoder and reconstructed output. This is a conceptual visualization, not a physical device or a scale drawing.

Signature: interruptible assembly/explosion of real Three.js geometry, bounded manual rotation, selectable layers. GSAP choreographs assembly and scroll orientation. Motion weighting: Jakub for restraint and production polish, Jhey for the central spatial demonstration. No elastic motion. Reduced motion is immediate, with all controls and labels retained.

Visitor path: inspect assembly → follow the full U-Net signal flow → explore 12 source features → understand training, weighted loss and independent validation → open the ocean dashboard. Shapes and claims follow architecture_design_v3_final.md. No invented performance achievements.

Quality bar: distinct labeled geometry, accurate tensor shapes and skip connections, sharp type and fine rules; no ornamental card grid or branding color; readable at 1440 and 1280 desktop plus a stacked mobile composition; working keyboard alternatives and a static schematic fallback for missing WebGL. Preserve existing landing and dashboard except route links.
